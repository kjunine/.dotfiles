kjunine's dotfiles
==================

## Mac OS X

```sh
xcode-select --install

git clone https://github.com/kjunine/.dotfiles.git ~/.dotfiles
cd ~/.dotfiles/macosx
```

## Ubuntu

```sh
sudo apt update
sudo apt upgrade

git clone https://github.com/kjunine/.dotfiles.git ~/.dotfiles
cd ~/.dotfiles/ubuntu
```
